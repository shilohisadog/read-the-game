{
  "name": "read-the-game",
  "private": true,
  "type": "module",
  "description": "Zero dependencies, deliberately. `npm test` runs node's built-in runner.",
  "scripts": {
    "test": "node --test test/",
    "build": "python3 builders/build_main.py && python3 builders/build_main.py --verify",
    "extract:verify": "python3 builders/extract.py --verify",
    "extract:validate": "python3 builders/extract.py --validate",
    "extract:vocab": "python3 builders/extract.py --vocab",
    "gates": "npm test && npm run extract:verify && npm run extract:validate && npm run extract:vocab && npm run build"
  }
}
