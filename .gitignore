# build output (regenerated from src/ + data/)
dist/

# scratch
*.tmp
*.bak
.DS_Store
__pycache__/
*.pyc

# python
__pycache__/

# Parked, unratified work — kept locally, deliberately NOT published.
# build_rules.py drafts the stoppage/"why the whistle" layer and its teaching
# copy, and leans on the "derived" doctrine category that docs/main-app-rework.md
# explicitly parks for Kevin and CHENG to settle. It was swept onto the remote by
# a `git add -A` in 3061347 and removed in the commit that follows; this entry is
# here so it cannot happen a third time by accident.
builders/build_rules.py
src/why-the-whistle.html

# fetch_nhl.py output — raw feeds belong in R2, never in the repo
ingest/
